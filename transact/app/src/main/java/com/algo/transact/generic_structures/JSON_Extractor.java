package com.algo.transact.generic_structures;

/**
 * Created by patilsp on 8/7/2017.
 */

public class JSON_Extractor {

/*    public static String extractShopItemIdAndVerify(String inputString, String moduleType, int moduleID)
    {
        JSONObject jObj = null;

        try {
            jObj = new JSONObject(inputString);
            String moduleType1 = jObj.getString(BarcodeDetails.MODULE_TYPE);
            int moduleId1 = jObj.getInt(BarcodeDetails.MODULE_ID);
            String itemId = jObj.getString(BarcodeDetails.ITEM_ID);

            if(moduleType1.equals(moduleType) && moduleId1==moduleID)
                return itemId;
        } catch (JSONException e) {
            e.printStackTrace();
        }
        return null;
    }*/

}
